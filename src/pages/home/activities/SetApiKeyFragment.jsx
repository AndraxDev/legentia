import React from 'react';
import PropTypes from "prop-types";

function SetApiKeyFragment({onNewIntent}) {
    return (
        <div></div>
    );
}

SetApiKeyFragment.propTypes = {
    onNewIntent: PropTypes.func.isRequired
}

export default SetApiKeyFragment;